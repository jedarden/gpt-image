# Example environment variables for GPT-Image-1 backend

# Required: OpenAI API key for image generation
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Port for backend server (default: 8080)
PORT=8080

# Optional: Path to frontend build directory (default: ../../frontend/build)
FRONTEND_BUILD_PATH=../../frontend/build